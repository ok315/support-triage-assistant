# Security Practices

API keys must be stored securely and should never be exposed in frontend applications.

Sandbox and production credentials should be stored separately.

Webhook requests should be verified using signature validation.

Enterprise customers have access to audit logs and role-based access control.

Security documentation describes recommended practices but does not guarantee regulatory compliance such as HIPAA or GDPR.